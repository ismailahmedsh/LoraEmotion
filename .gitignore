# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# Environment / secrets
.env

# Model weights (store on HuggingFace Hub, not git)
*.pt
*.bin
*.safetensors
*.gguf

# Datasets (too large for git — use HF datasets or local only)
data/raw/
data/processed/

# Jupyter / Colab
.ipynb_checkpoints/
*.ipynb_checkpoints

# Claude Code / internal dev notes
.claude/
CLAUDE.md
CONTEXT.md

# Bootstrap scripts — temporary Claude API scaffolding, never committed
mood_classifier/bootstrap/
evolution/bootstrap_scripts/

# Internal — tests and subfolder READMEs not for public repo
tests/
adapters/README.md
data/README.md
notebooks/README.md
router/README.md
training/README.md

# Router runtime artifacts (built locally, not committed)
router/embeddings/*.npy
router/unknown_buffer.jsonl

# OS
.DS_Store
